using UnityEngine;
using System.Collections;

public class wallget : MonoBehaviour {

	public static GameObject[] walls;

	// Use this for initialization
	void Start () {
	
		/*walls = new GameObject[9];
		walls = GameObject.FindGameObjectsWithTag ("Untagged");

		if (walls == null)
			Debug.Log ("Still NULL");
		else {
			Debug.Log (walls.Length);
		}*/

	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
